syntax = "proto3";
package users;

option go_package = "/users";

message User {
    string id = 1;
    string name = 2;
    string email = 3;
}

service UserService {
    rpc Add(User) returns (User);
}