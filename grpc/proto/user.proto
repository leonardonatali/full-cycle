syntax = "proto3";
package pb;

option go_package = "github.com/leonardonatali/full-cycle/grpc/pkg/protobuf/users";

message User {
    string id = 1;
    string name = 2;
    string email = 3;
}

service UserService {
    rpc Add(User) returns (User);
}